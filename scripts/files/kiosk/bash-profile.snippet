if [ -z "$DISPLAY" ] && [ "$(tty)" = "/dev/tty1" ]; then
  # Generic X11 launcher (Pi OS Lite, most Armbian tty1-X images) — a
  # fallback, not a hard assumption every Armbian image behaves the same.
  startx -- -nocursor
fi

# Forces HDMI output on (screen stays active even with no monitor
# attached at boot — plugging one in later works without a reboot) and
# silences the boot splash/delay for a kiosk display.
hdmi_force_hotplug=1
hdmi_force_hotplug:0=1
hdmi_force_hotplug:1=1
disable_splash=1
boot_delay=0
